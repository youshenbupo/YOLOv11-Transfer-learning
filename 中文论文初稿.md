# 基于排序式测试时校准的 YOLO11 无监督域适应目标检测方法研究

## 摘要

无监督域适应目标检测旨在将源域有标注数据上训练得到的检测器迁移到无标注目标域。现有方法通常依赖复杂的域对齐、教师学生结构或重型预训练模块，但这些设计在实际 YOLO 风格检测框架中未必稳定有效。本文围绕一个更务实的问题展开：在不显著增加训练复杂度的前提下，测试时置信度校准能否提升 YOLO11 自训练框架在目标域上的检测性能与置信度可靠性。为此，本文构建了一个基于 YOLO11 的无监督域适应目标检测基线，并提出一种基于多视图一致性与风险排序的测试时校准方法。该方法首先对目标域图像生成多个轻量视图，利用跨视图检测一致性估计每个预测框的可靠性，再对高置信但低一致性的高风险检测进行选择性置信度下调。实验在 `SIM10K -> Cityscapes` 与 `Cityscapes -> Foggy Cityscapes` 两个经典 benchmark 上进行。结果表明，在 `SIM10K -> Cityscapes` 上，所提方法将目标域 AP50 从 `32.46` 提升到 `37.15`，AP50-95 从 `18.70` 提升到 `21.17`，同时在 `ECE`、`NLL` 与 `Brier score` 上均获得小幅改善；在 `Cityscapes -> Foggy Cityscapes` 上，该方法与 baseline 基本持平，但保持了更好的置信度校准指标。进一步的消融实验表明，若干更复杂的伪标签筛选、对比学习预训练与 EMA teacher 变体均未优于该简单校准基线。实验结果说明，在当前 YOLO11 自训练域适应检测框架中，低成本的测试时校准比更重的适应模块更稳定、更具实用价值。

**关键词：** 无监督域适应目标检测；YOLO11；自训练；测试时校准；一致性建模；风险排序

---

## 1 引言

目标检测模型通常对域偏移敏感。一个在合成数据、晴天数据或特定相机平台上训练的检测器，在真实场景、恶劣天气或不同采集设备上的性能往往显著下降。无监督域适应目标检测通过使用源域标注数据与目标域无标注图像来缓解这一问题，是自动驾驶视觉感知中的重要研究方向。

近年来，该领域出现了大量方法，包括基于对抗学习的特征对齐、基于教师学生结构的伪标签自训练、基于一致性约束的上下文建模，以及视觉语言先验驱动的适配策略。然而，对于以 YOLO 为代表的单阶段检测框架而言，复杂的适应模块并不必然带来目标域性能提升。一方面，目标域伪标签本身存在明显噪声；另一方面，模型在域偏移条件下的置信度往往不可靠，容易出现“高分错误框”主导伪标签传播的问题。

本项目最初实现了一个较宽的研究框架，包括对比学习预训练、渐进式伪标签筛选、域感知损失、注意力模块、LayerLock 风格渐进冻结策略以及测试时校准等多个方向。经过正式 benchmark 实验后，实验事实表明：大多数复杂分支要么未带来目标域收益，要么在目标域 AP 上明显退化；真正稳定有效的方法是一个轻量的 **rank-based test-time calibration** 模块。

基于这一实验结论，本文不再试图提出一个新的全栈式 DAOD 框架，而是聚焦于一个更明确也更可验证的问题：

> 在 YOLO11 自训练无监督域适应目标检测框架中，基于多视图一致性的排序式测试时校准是否能够比更复杂的适应分支更稳定地提升目标域检测性能？

围绕该问题，本文的主要工作如下：

1. 构建了一个基于 YOLO11 的无监督域适应目标检测 baseline，采用源域监督训练与目标域伪标签自训练的标准流程；
2. 提出一种基于多视图一致性与风险排序的测试时校准方法，仅对高风险预测进行选择性置信度修正；
3. 在两个经典 benchmark 上对该方法进行验证，并对多种伪标签增强与预训练分支进行消融分析；
4. 证明在当前实现条件下，轻量置信度校准比注意力模块、LayerLock 风格冻结与当前版本的对比学习预训练更稳定有效。

---

## 2 相关工作

### 2.1 域对齐方法

早期无监督域适应目标检测主要采用特征级域对齐思想。典型代表是 Domain Adaptive Faster R-CNN，其在图像级与实例级上同时进行域分类对抗学习，并通过一致性正则化稳定候选区域分布。这类方法的核心思想是使源域与目标域的特征分布在共享特征空间中更加接近。

### 2.2 自训练与教师学生方法

随着伪标签技术的发展，越来越多方法转向教师学生框架与自训练范式。Adaptive Teacher 利用弱增强/强增强、伪标签监督与 EMA 更新构建跨域教师学生检测器，在 DAOD 中取得了有代表性的结果。此类方法的关键在于如何提高目标域伪标签质量，并降低错误伪标签在训练中被反复强化的风险。

### 2.3 一致性建模方法

一致性学习是另一条重要路线。2PCNet、SOCCER、MIC 等方法都强调在不同视图、不同上下文或不同阶段之间保持检测结果的一致性。与单纯的域对齐相比，这类方法更关注目标域伪标签或预测结果在扰动条件下的稳定性。

### 2.4 当前工作的位置

本文的方法与上述一致性建模方法最为接近，但本文并未引入新的重型特征对齐分支，也未构建完整的复杂教师学生训练框架。相反，本文将**目标域置信度可靠性**本身作为主要研究对象，试图通过测试时的一致性估计与风险排序，对高风险错误框进行修正，从而在不大幅增加训练复杂度的前提下获得更稳定的目标域收益。

---

## 3 方法

### 3.1 问题定义

设源域有标注数据集为：

$$
\mathcal{D}_s = \{(x_i^s, y_i^s)\}_{i=1}^{N_s}
$$

设目标域无标注数据集为：

$$
\mathcal{D}_t = \{x_j^t\}_{j=1}^{N_t}
$$

其中，$x_i^s$ 为源域图像，$y_i^s$ 为对应检测标注，$x_j^t$ 为目标域无标注图像。

设检测器为：

$$
f_{\theta}(x)=\{(b_k,p_k,c_k)\}_{k=1}^{M}
$$

其中，$b_k$ 表示预测框，$p_k$ 表示预测置信度，$c_k$ 表示类别预测结果，$\theta$ 为模型参数。

本文目标是在不使用目标域人工标注的条件下，学习参数 $\theta$，使模型在目标域上具有更好的检测性能与置信度可靠性。

### 3.2 YOLO11 自训练 baseline

本文的 baseline 采用 YOLO11 检测器与单轮自训练框架。

源域监督损失表示为：

$$
\mathcal{L}_s(\theta)=\mathcal{L}_{det}(f_{\theta}(x^s), y^s)
$$

对目标域图像生成伪标签：

$$
\hat{y}^t=\mathcal{P}(f_{\theta}(x^t))
$$

其中，$\mathcal{P}(\cdot)$ 表示伪标签生成算子。

随后构造混合训练目标：

$$
\mathcal{L}(\theta)=\mathcal{L}_s(\theta)+\lambda_t \mathcal{L}_p(\theta)
$$

其中：

$$
\mathcal{L}_p(\theta)=\mathcal{L}_{det}(f_{\theta}(x^t), \hat{y}^t)
$$

该 baseline 的主要问题在于：目标域伪标签由域偏移下的模型直接产生，容易包含大量高置信误检。

### 3.3 多视图一致性建模

为估计目标域检测可靠性，本文在测试时对同一目标域图像构造一组轻量视图：

$$
\mathcal{V}(x)=\{v_0(x), v_1(x), \dots, v_K(x)\}
$$

其中：

- $v_0(x)=x$ 为原图；
- 其余视图为轻量扰动，如水平翻转、亮度变化、对比度变化等。

在每个视图上进行检测：

$$
f_{\theta}(v_k(x))=\{d_m^{(k)}\}
$$

对原图上的某个基础检测框 $d$，在其他视图中寻找映射回原图坐标系后的最佳匹配框 $d_k^\ast$。

### 3.4 一致性分数

对第 $k$ 个视图，定义匹配分数为：

$$
s_k(d)=0.5 \cdot \operatorname{IoU}(d,d_k^\ast)+0.5 \cdot \left(1-\left|p(d)-p(d_k^\ast)\right|\right)
$$

则总体一致性分数为：

$$
c(d)=\frac{1}{K}\sum_{k=1}^{K}s_k(d)
$$

若 $c(d)$ 较高，则说明该检测在轻微扰动下具有稳定性；若 $c(d)$ 较低，则说明该检测更可能不可靠。

### 3.5 风险分数

为了重点关注“高置信但不稳定”的危险框，本文定义风险分数为：

$$
r(d)=p(d)\bigl(1-c(d)\bigr)
$$

该定义具有清晰的物理意义：

- 若检测框置信度高但一致性差，则风险高；
- 若检测框置信度低，则即使一致性差，对伪标签传播的破坏也相对有限；
- 若检测框一致性高，则被视为较可信预测。

### 3.6 排序式测试时校准

根据风险分数 $r(d)$ 对所有检测框降序排序，选出高风险集合 $\mathcal{R}_{\alpha}$。随后仅对这部分高风险框进行置信度修正：

$$
p'(d)=
\begin{cases}
p(d)\bigl(\alpha + \beta c(d)\bigr), & d \in \mathcal{R}_{\alpha} \text{ 且 } c(d)<\tau_c \\
p(d), & \text{其他情况}
\end{cases}
$$

其中：

- $\alpha$ 为基础缩放系数；
- $\beta$ 为一致性权重；
- $\tau_c$ 为一致性阈值。

与全局 temperature scaling 不同，本文并不修改所有检测，而是仅对**高风险且低一致性**的检测进行选择性下调。这使得方法更加保守，也更适合目标域自训练场景。

### 3.7 校准引导的伪标签扩展

在主方法基础上，本文还测试了两种伪标签增强方案。

#### 3.7.1 校准引导的伪标签筛选

定义伪标签保留函数：

$$
g(d)=\mathbb{1}[p'(d)\ge \tau_p]\cdot \mathbb{1}[c(d)\ge \tau_{cons}]\cdot \mathbb{1}[r(d)\le \tau_{risk}]
$$

则目标域伪标签集合为：

$$
\hat{y}^t=\{d \mid g(d)=1\}
$$

该方法联合使用校准后置信度、一致性和风险分数过滤伪标签。

#### 3.7.2 自适应伪标签阈值

进一步定义检测相关的动态阈值：

$$
\tau_{eff}(d)=\tau_0+\gamma \max(0, c_{target}-c(d))
$$

并使用如下规则保留伪标签：

$$
g(d)=\mathbb{1}[p'(d)\ge \tau_{eff}(d)]
$$

该机制会对一致性较低的检测提高阈值，使伪标签筛选更为保守。

---

## 4 实验设计

### 4.1 数据集与任务

实验在两个经典 DAOD benchmark 上进行：

1. `Cityscapes -> Foggy Cityscapes`
2. `SIM10K -> Cityscapes`

其中：

- `Cityscapes -> Foggy` 表示从晴天真实图像向雾天真实图像迁移；
- `SIM10K -> Cityscapes` 表示从合成驾驶图像向真实街景图像迁移。

### 4.2 评价指标

本文报告以下指标：

- 目标域 AP50；
- 目标域 AP50-95；
- 置信度校准指标：`ECE`、`NLL`、`Brier score`。

需要强调的是，本文最终以**目标域 AP**作为方法选择的主要依据，而不再以 source 验证 AP 作为主判断标准。这是因为实验中已经观察到：某些方法在源域上指标正常，但在目标域上几乎崩溃。

---

## 5 实验结果与分析

### 5.1 主结果

| Benchmark | Method | Target AP50 | Target AP50-95 | $\Delta$ECE | $\Delta$NLL | $\Delta$Brier |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Cityscapes $\rightarrow$ Foggy | baseline | 0.1433 | 0.0871 | - | - | - |
| Cityscapes $\rightarrow$ Foggy | rank_calibration | 0.1418 | 0.0873 | -0.0000038 | -0.0000248 | -0.0000110 |
| SIM10K $\rightarrow$ Cityscapes | baseline | 0.3246 | 0.1870 | - | - | - |
| SIM10K $\rightarrow$ Cityscapes | rank_calibration | 0.3715 | 0.2117 | -0.0000184 | -0.0000462 | -0.0000134 |

结果表明：

1. 在 `SIM10K -> Cityscapes` 上，`rank_calibration` 明显优于 baseline；
2. 在 `Cityscapes -> Foggy` 上，`rank_calibration` 与 baseline 基本持平，但保持了更好的校准指标；
3. 因而该方法更适合被解释为一种**低成本、稳定的目标域改进模块**，而不是一个在所有 benchmark 上全面领先的框架。

### 5.2 消融结果

#### 5.2.1 `SIM10K -> Cityscapes`

| Method | Target AP50 | Target AP50-95 | 结论 |
| --- | ---: | ---: | --- |
| baseline | 0.3246 | 0.1870 | 对照组 |
| rank_calibration | 0.3715 | 0.2117 | 当前最佳 |
| calibration_guided_pseudo | 0.3715 | 0.2117 | 稳定，但未超过主方法 |
| adaptive_pseudo_threshold | 0.3715 | 0.2117 | 稳定，但未超过主方法 |
| progressive_pseudo | 0.2833 | 0.1739 | 不如 baseline |
| domain_loss_pretrain | 0.0208 | 0.0127 | 目标域崩溃 |
| cbam_pretrain | 0.0153 | 0.0104 | 目标域崩溃 |
| layerlock_pretrain | 0.0035 | 0.0017 | 目标域崩溃 |
| ema_teacher | 0.0062 | 0.0020 | 当前实现不可用 |

#### 5.2.2 `Cityscapes -> Foggy`

| Method | Target AP50 | Target AP50-95 | 结论 |
| --- | ---: | ---: | --- |
| baseline | 0.1433 | 0.0871 | 当前最佳 |
| rank_calibration | 0.1418 | 0.0873 | 与 baseline 基本持平 |
| progressive_pseudo | 0.1374 | 0.0834 | 略差 |
| cbam_pretrain | 0.0516 | 0.0302 | 效果较差 |
| domain_loss_pretrain | 0.0345 | 0.0192 | 效果较差 |
| layerlock_pretrain | 0.0160 | 0.0091 | 效果较差 |

### 5.3 结果讨论

从实验结果可以得出三个明确结论。

第一，**rank-based test-time calibration 是当前项目中最可信的有效模块**。它在 `SIM10K -> Cityscapes` 上显著提升目标域 AP，并在校准指标上保持一致改善。

第二，**更复杂的方法并不一定更好**。无论是 CBAM、LayerLock 风格冻结、域感知对比学习，还是当前版本的 EMA teacher，都未在目标域 AP 上给出有竞争力的结果。

第三，**source 指标会误导方法选择**。一些分支在 source 验证 AP 上看似正常，但在目标域上明显崩溃，因此后续研究必须以目标域 AP 和可靠性指标作为主判断依据。

---

## 6 与前沿方法的差距

以 `SOCCER` 等代表性文献作为参照，本项目仍与前沿方法存在明显差距。

| Benchmark | 本项目最佳 AP50 | 参考方法 AP50 | 差距 |
| --- | ---: | ---: | ---: |
| Cityscapes $\rightarrow$ Foggy | 0.1433 | 0.511 | 0.3677 |
| SIM10K $\rightarrow$ Cityscapes | 0.3715 | 0.638 | 0.2665 |

这说明本文当前结果并不足以支持“新的 SOTA DAOD 方法”这一表述。更合理的定位应是：

- 面向 YOLO11 自训练 DAOD 的校准研究；
- 一个小而清晰、可复现的目标域增益模块；
- 以及对多种复杂分支失效原因的系统性分析。

---

## 7 结论

本文围绕 YOLO11 自训练无监督域适应目标检测框架，研究了测试时置信度校准在目标域中的作用。针对域偏移下高置信误检容易污染目标域伪标签与检测结果的问题，本文提出一种基于多视图一致性与风险排序的测试时校准方法。实验表明，该方法在 `SIM10K -> Cityscapes` 上能够同时提升目标域检测性能与置信度可靠性，在 `Cityscapes -> Foggy` 上则与 baseline 基本持平但保持更优校准表现。进一步的消融实验表明，多种更复杂的伪标签增强、对比预训练与教师学生变体均未超越这一简单校准基线。综合来看，在当前 YOLO11 DAOD 设置中，轻量测试时校准比额外适应复杂度更值得优先研究。

---

## 参考文献线索

后续正式写作时，建议重点整理以下论文：

1. Domain Adaptive Faster R-CNN  
2. Adaptive Teacher  
3. SOCCER  
4. MIC  
5. ALDI / ALDI++  
6. 2PCNet  
7. AdvMix  
8. CAGD  
9. DA-Ada

这些文献的方法路线已经在归档文件中总结：

- [FRONTIER_LITERATURE_SURVEY.md](D:\yolo\yolov7-main\docs\archive_markdown\FRONTIER_LITERATURE_SURVEY.md)
